apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  DB_USER: {{ .Values.database.user }}
  DB_PASSWORD: {{ required "database.password is required" .Values.database.password }}
